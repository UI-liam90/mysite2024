---
import { WpImage } from "~helpers/WpImage";
import "./style.scss";

const { blockData, blockContext } = Astro.props;
---

<div class:list="image-block">
    {blockData.image && <WpImage file={blockData.image.node} />}
</div>
